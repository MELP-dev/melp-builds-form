/// <reference types="react" />
import { MpControlProps } from "../../types/Form";
export default function Group({ control, size, layout, variant, register, locale, errors, hookFormControl, defaultMessages, overridables, }: MpControlProps): JSX.Element;
